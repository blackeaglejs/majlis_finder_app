<% if user_signed_in? and current_user.profile %>
  Signed in as <%= current_user.profile.name %>
<% elsif user_signed_in? %>
  Signed in as <%= current_user.email %>.
<% end %>

<h1>Majlis Finder</h1>

The best place to find a majlis!

<% if !user_signed_in? %>
  <p>New to Majlis Finder? <%= link_to 'Sign Up!', new_user_registration_path %></p>
  <p>Already a member? <%= link_to 'Sign In', new_user_session_path %></p>
<% else %>
  <% if !current_user.profile %>
    <p><%= link_to 'Create Your Profile', new_profile_path %></p>
  <% elsif current_user.profile %>
    <p><%= link_to 'View Your Profile', profile_path(@profile.id) %></p>
  <% end %>
  <p><%= link_to 'Sign Out', destroy_user_session_path, :method => :delete %></p>
<% end %>

